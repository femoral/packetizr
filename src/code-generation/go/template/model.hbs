package packets

{{#if isPacket}}const {{ className }}Header = uint16({{header}})

{{/if}}
type {{ className }}{{#unless isPacket}}Dto{{/unless}} struct {
{{#each fields as |field|}}
    {{field.name}} {{#if isObject}}*{{/if}}{{field.type}}
{{/each}}
}